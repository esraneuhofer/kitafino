import { IbanFormatDirective } from './iban-format.directive';

describe('IbanFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new IbanFormatDirective();
    expect(directive).toBeTruthy();
  });
});
